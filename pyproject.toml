[project]
name = "displaycli"
version = "0.1.0"
description = "A CLI tool to manage display resolutions on macOS."
requires-python = ">=3.8"
dependencies = [
    "nercone-modern",
    "pyobjc"
]

[project.scripts]
displaycli = "displaycli.cli:main"
